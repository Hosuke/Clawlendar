interface:
  display_name: "Clawlendar"
  short_description: "Timestamp-first perpetual calendar interop for AI agents"
  default_prompt: "Use $clawlendar to normalize timestamps and convert projected dates across calendars with stable JSON."
