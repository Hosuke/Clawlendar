interface:
  display_name: "Clawlender"
  short_description: "Timestamp-first perpetual calendar interop for AI agents"
  default_prompt: "Use $clawlender to normalize timestamps and convert projected dates across calendars with stable JSON."
